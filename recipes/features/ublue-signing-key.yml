# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
modules:

  # Trust Sigstore signing key from https://github.com/rrenomeron/ublue-tr
  # Remove when images built form ublue-tr repository are no longer available
  # on ghrc.io
  - type: script
    scripts:
      - add-ublue-tr-sigstore-key.sh