modules:
  - type: files
    files:
      - source: system
        destination: / 
  - type: script
    snippets:
    # We run this to put get our home CA in the system circle of trust
      - "update-ca-trust extract"        