---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: centos-stream-bootc-workstation-tr
# description will be included in the image's metadata
description: CentOS Stream 10 with my personal defaults, in a bootc image
alt-tags:
  - "10"
  - stream10
  - latest

# the base image to build on top of (FROM) and the version tag to use
base-image: quay.io/centos-bootc/centos-bootc
image-version: stream10

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / 
      - source: system-stream10
        destination: /
  - from-file: features/el10-base-desktop.yml
  - from-file: features/workarounds.yml
  - from-file: features/default-flatpaks.yml
  - from-file: features/chrome.yml
  - from-file: features/virtualization.yml
  - from-file: features/toolbox-runtimes.yml
  - from-file: features/docker.yml
  - from-file: features/bluefin-parity.yml
  - from-file: features/ui-fixes.yml
  - from-file: features/ui-enable-logo-menu.yml
  - from-file: features/vscode.yml

  - type: signing # this sets up the proper policy & signing files for signed images to work fully

